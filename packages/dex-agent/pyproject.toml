[project]
name = "dex-agent"
version = "0.1.0"
description = "Dex — AI agent logic, tools, and models."
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
